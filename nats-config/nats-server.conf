websocket: {
  port: 8080,
  no_tls: true // For local development, no HTTPS needed for ws://
  // For local development, we'll rely on NATS default open permissions.
  // In a production setup, you would add an 'authorization' block here
  // to control JWT validation and user permissions.
}

// You can also configure the monitoring port if needed, though 8222 is default.
// http_port: 8222

// NATS client port will default to 4222 unless changed here.
// port: 4222
