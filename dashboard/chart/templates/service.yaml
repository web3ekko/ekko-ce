apiVersion: v1
kind: Service
metadata:
  name: {{ include "ekko-dashboard.fullname" . }}
  labels:
    {{- include "ekko-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ekko-dashboard.selectorLabels" . | nindent 4 }}